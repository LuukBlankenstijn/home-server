---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pg-data
  namespace: deurmat
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
